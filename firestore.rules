rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Foxty memories collection - managed by server-side backend
    match /foxty_memories/{memoryId} {
      // Allow read/write for server backend; deny unauthenticated public clients
      allow read, write: if request.auth != null;
    }
  }
}
